name: Nodejs setup

on: push

jobs: 
  nodejs-practice:
    runs-on: ubuntu-latest

    steps:
      - name: Before checkout
        run: ls

      - name: Checkout code
        uses: actions/checout@v4

      - name: After checkout
        run: ls